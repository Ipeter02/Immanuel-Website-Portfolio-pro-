# Server Configuration
PORT=3000

# Database (Optional - falls back to local file if missing)
MONGO_URI=

# Email Configuration (Required for sending emails and replies)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Admin email to receive notifications (defaults to SMTP_USER)
ADMIN_EMAIL=your-email@gmail.com

# Client-Side Configuration
VITE_USE_CUSTOM_SERVER=true
VITE_CUSTOM_API_URL=/api
