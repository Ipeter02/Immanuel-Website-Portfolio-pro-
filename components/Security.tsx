import React from 'react';

// Security features disabled to prevent browser freezing/performance issues
// The aggressive debugger loop was causing the application to hang.
const Security: React.FC = () => {
  return null;
};

export default Security;